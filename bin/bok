#!/usr/bin/env node
"use strict"

require("babel/register")

var path = require("path")
var argv = require("yargs").argv

// Require the task module
require(path.join(process.cwd(), argv.A))

// Run the task queue
require("../lib").run()
